const pen = {brand: 'econo', price: 10, color: 'black'}
const pencil = new objecct()
console.log(pencil)

const rubber = objecct.create({})
console.log(rubber)


const pen = {brand: 'econo', price: 10, color: 'black'} 
// This creates an object 'pen' with three properties: brand, price, and color.

const pencil = new Object() 
// This creates an empty object 'pencil' using the Object constructor.
console.log(pencil) 
// This will print the empty object 'pencil'.

const rubber = Object.create({})
// This creates an empty object 'rubber' using Object.create, inheriting from an empty object.
console.log(rubber) 
// This will print the empty object 'rubber'.
